name: scholar_time_table_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  faker: ^2.1.0
  uuid: ^4.3.2
  get_it: ^7.2.0
  path_provider: ^2.0.8
  time_planner: ^0.1.2+1

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/src/core/resource/data/Courses.json